{
  "name": "ocr-gateway",
  "version": "1.0.0",
  "scripts": {
    "start": "npx -y serve web -p 3000",
    "dev": "npx -y serve web -p 3000",
    "build": "echo 'No build required'"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.2",
    "tsx": "^4.7.1",
    "concurrently": "^8.2.2"
  }
}
